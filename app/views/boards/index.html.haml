%h1 HOME
.container
  = link_to @board.name
  = link_to @board.content
  -# - @boards.each do |board|
  -#   = render 'commons/board', board: board
  -# = link_to new_board_path do
  -#   .float_btn
  -#     %i.fa.fa-plus
